#include <stdio.h>

/**
* main - Displays the name of the program
* @argc : Number of arguments (not used here)
* @argv : Array of arguments
*
* Return: Always 0
*/

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
