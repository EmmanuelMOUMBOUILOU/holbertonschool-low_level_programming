#include "main.h"
#include <ctype.h>

/**
 * add - adds two integers and returns the result
 * @x: the integer to be checked
 * @y: the integer to be checked
 *
 * Return: add of x and y
 */
int add(int x, int y)
{
	return (x + y);
}
